<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chat.mapper.ChatMsgMapper">
  <insert id="InsertChatMsg" parameterType="com.chat.bean.ChatMsg">
    insert into tb_disable_date_message (sender_id, receiver_id,  message_type, send_text)
    values (#{senduserid}, #{reciveuserid}, #{msgtype}, #{sendtext})
  </insert>


  <select id="LookTwoUserMsg" resultType="com.chat.bean.ChatMsg">
    select * from tb_disable_date_message where
      sender_id = #{chatMsg.senduserid} and receiver_id = #{chatMsg.reciveuserid} or
      sender_id = #{chatMsg.reciveuserid} and receiver_id = #{chatMsg.senduserid}
  </select>

  <select id="LookPersonUserMsg" resultType="com.chat.bean.ChatMsg">
    select * from tb_disable_date_message where
      sender_id = #{chatMsg.senduserid} and receiver_id = #{chatMsg.reciveuserid}
  </select>

</mapper>